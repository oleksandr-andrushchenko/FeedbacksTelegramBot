{% trans_default_domain 'tg.premium' %}
{{ 'icon.info'|trans({}, 'tg') }} {{ 'title'|trans }}

{% for name,command in commands %}
{% include 'command.html.twig' with {name: name, command: command.command} %}:

{% for period,limit in command.limits %}
 - {{ 'limit_item'|trans({'%period%': period|trans({}, 'tg'), '%limit%': limit})|raw }}
{% endfor %}

{% endfor %}
{{ 'icon.info'|trans({}, 'tg') }} {{ 'summary'|trans }}