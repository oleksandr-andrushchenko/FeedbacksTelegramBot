{% trans_default_domain 'telegram.start' %}
{{ 'icon.info'|trans({}, 'telegram') }} {{ 'title'|trans }}

{{ 'icon.attention'|trans({}, 'telegram') }} {{ 'agreements'|trans({'%privacy_policy_link%': privacy_policy_link, '%terms_of_use_link%': terms_of_use_link})|raw }}

{{ 'commands_title'|trans }}

{% for name,command in commands %}
{% include 'command.html.twig' with {name: name, command: command} %}:

{{ name|trans }}

{% endfor %}
{{ 'icon.info'|trans({}, 'telegram') }} {{ 'summary'|trans }}