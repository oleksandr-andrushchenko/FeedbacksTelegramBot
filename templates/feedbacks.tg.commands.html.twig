{% trans_default_domain 'feedbacks.tg.commands' %}
ℹ️ <u><b>{{ 'items'|trans }}</b></u>:

{% for name,command in commands %}
▫️ {% include 'feedbacks.tg.command.html.twig' with {name: name, html: true, link: true} %}:
{{ name|trans }}
{% endfor %}

⚠️ {{ 'summary'|trans({'%button_icon%': '🎛️'}) }}