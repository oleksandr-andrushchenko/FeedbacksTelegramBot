{% trans_default_domain 'tg.feedback' %}
{% if number %}
{{ 'icon.number'|trans({}, 'tg') ~ number }}:
{% endif %}
{{ created_at }}, {{ 'somebody_from'|trans }} <u>{{ country }}</u> {{ 'wrote_about'|trans }} "<u>{{ search_term }}</u>":
"<b><tg-spoiler>{{ description ?? ('(' ~ 'no_details'|trans ~ ')') }}</tg-spoiler> {{ ('rating.' ~ rating.name)|trans({'%rating%': rating.value}, 'tg') }}</b>"
