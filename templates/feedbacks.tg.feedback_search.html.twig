{% trans_default_domain 'feedbacks.tg.feedback_search' %}
{% if number %}
    {{ 'icon.number'|trans({}, 'feedbacks.tg') ~ number }}:
{% endif %}
{{ created_at }}, {{ 'somebody_from'|trans }} <u>{{ country }}</u> {{ 'searched_for'|trans }} "<u>{{ search_term }}</u>"
