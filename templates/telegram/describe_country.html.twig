ℹ️ {{ transTelegram('feedbacks.view.country.title', language_code) }}

{% apply spaceless %}
{% if country %}
    {{ transTelegram('feedbacks.view.country.your_country_known', language_code, {icon: icon, name: name}) }}
{% else %}
    {{ transTelegram('feedbacks.view.country.your_country_known', language_code, {icon: '🌎'}) }}
{% endif %}
{% endapply %}


{{ transTelegram('feedbacks.view.country.summary', language_code) }}