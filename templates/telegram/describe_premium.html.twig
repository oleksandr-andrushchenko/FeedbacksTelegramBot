ℹ️ {{ transTelegram('feedbacks.view.premium.title', language_code) }}

<u>✏️ {{ transTelegram('feedbacks.view.premium.create_title', language_code) }}</u>
{% for period,limit in create_limits %}
    - {{ transTelegram('feedbacks.view.premium.list_item', language_code, {period: period|trans({}, null, language_code), limit: limit})|raw }}
{% endfor %}

<u>🔍 {{ transTelegram('feedbacks.view.premium.search_title', language_code) }}</u>
{% for period,limit in search_limits %}
    - {{ transTelegram('feedbacks.view.premium.list_item', language_code, {period: period|trans({}, null, language_code), limit: limit})|raw }}
{% endfor %}

{{ transTelegram('feedbacks.view.premium.summary', language_code) }}