search_term:
  text:
    not_blank: Поисковый запрос не может быть пустым.
    min_length: Поисковый запрос должен содержать не менее {{ limit }} символов.
    max_length: Поисковый запрос не может быть длиннее {{ limit }} символов.
  messenger_profile_url:
    url: URL-адрес профиля не является допустимым URL-адресом.
create_feedback:
  rating:
    not_null: Оценка не может быть пустой.
  description:
    min_length: Описание должно содержать не менее {{ limit }} символов.
    max_length: Описание не может быть длиннее {{ limit }} символов.